name: lua-compat-matrix
on: [push, pull_request]
jobs:
  test:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        lua: ["5.1", "5.2", "5.3", "5.4"]
    steps:
      - uses: actions/checkout@v4
      - uses: ilammy/msvc-dev-cmd@v1
      - name: Cache FetchContent Lua
        uses: actions/cache@v4
        with:
          path: build/_deps
          key: lua-${{ matrix.lua }}-deps-${{ runner.os }}
      - name: Configure
        # Quote -DLUA_VERSION: Windows pwsh splits 5.4 at the dot.
        # VS generator matches local build/msvc (Ninja is not on PATH by default).
        # /3rd is gitignored: no vendored Lua sources on a clean checkout.
        # LuaSelect FetchContents lua.org tarballs for every matrix version,
        # including 5.4 when 3rd/lua-5.4.8 is absent.
        run: cmake -S . -B build -A x64 "-DLUA_VERSION=${{ matrix.lua }}"
      - name: Build
        run: cmake --build build --config Release --target lua asyncsocket luadap
      - name: Stage bin for tests
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          New-Item -ItemType Directory -Force -Path bin | Out-Null
          $src = $null
          foreach ($d in @("build/bin", "bin")) {
            if (Test-Path (Join-Path $d "lua.exe")) {
              $src = $d
              break
            }
          }
          if (-not $src) { throw "lua.exe not found after build (tried build/bin and bin)" }
          if ($src -ne "bin") {
            Copy-Item -Force (Join-Path $src "lua.exe") bin/
            Copy-Item -Force (Join-Path $src "asyncsocket.dll") bin/
            Copy-Item -Force (Join-Path $src "luadap.dll") bin/
          }
          foreach ($name in @("lua.exe", "asyncsocket.dll", "luadap.dll")) {
            if (-not (Test-Path (Join-Path "bin" $name))) {
              throw "missing bin/$name after stage (src=$src)"
            }
          }
      - name: Tests
        shell: pwsh
        run: |
          $env:PATH = "$pwd/bin;$env:PATH"
          python script/test/test_asyncsocket_smoke.py
          python script/test/test_dap_luadap_handshake.py
          python script/test/test_dap_evaluate.py
          python script/test/test_dap_condition.py
